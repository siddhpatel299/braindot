revert: bring back the full app preview in the hero

Removing it was an overcorrection. The only real problem with the original
was the stale "second-brain" title bar, which is a one-line fix — not a
reason to throw away a mockup that shows the whole product (file tree,
tabbed editor, note metadata, and the AI suggestion panel) in one glance.

The simpler animated note demo that replaced it showed less and read as a
widget rather than an application.

- restored at its original 880px, title bar now reads "braindot"
- sits 40px under the CTAs, visible without scrolling
- removed the now-dead demo markup, CSS and script
